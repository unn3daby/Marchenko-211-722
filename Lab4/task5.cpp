#include <iostream>
#include <math.h>
int main()
{
    using namespace std;
    float a, b;
    cin >> a >> b;
    cout << "sum = " << a + b << endl << "raznost' = " << a - b << endl << "proizved = " << a * b << endl << "chast moduley = " << abs(a) / abs(b) << endl;
}