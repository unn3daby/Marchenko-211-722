#include <iostream>
int main() {
	using namespace std;
	double a, b;
	cin >> a >> b;
	double c = a + b;
	double d = a - b;
	double e = a * b;
	double f = (a * a) / (b * b);
	cout << c << endl << d << endl << e << endl << f;
}